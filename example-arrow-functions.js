// Chalange Area
function add (a, b){
    return a+b;
};

// AddStatment
var addStatment = (a, b) => {
    return a+b;
};

console.log(addStatment(4,2));

// Add Expression
var addExpression = (a, b) => a + b;

console.log(addExpression(3,2));

console.log(add(3,4));
console.log(add(5,6));









// var names = ['Andrew', 'Faza','Galih'];

// names.forEach(function(name){
//     console.log('forEach: ', name)
// });

// names.forEach((name)=>{
//     console.log('ArrowFunc', name);
// });

// names.forEach((name)=>console.log(name));

// var returnMe = (name) => name + '!';
// console.log(returnMe('Panji'));

// var person = {
//     name: 'Panji',
//     greet: function(){
//         names.forEach((name) => {
//             console.log(this.name + ' Say hi to ' + name)
//         })
//     }
// }

// person.greet();